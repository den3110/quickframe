import Layout from "../Layout";
import { FlexRowAlign } from "components/flexbox";
const TelegramChannelSignalStrategy = () => {
  return <Layout>
      <FlexRowAlign mt={8}>There is no conversation 11</FlexRowAlign>
    </Layout>;
};
export default TelegramChannelSignalStrategy;